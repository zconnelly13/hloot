build:
  docker:
    web: Dockerfile
    release: Dockerfile
    worker: DockerfileWorker
run:
  web: sh -c "python manage.py runserver 0.0.0.0"
  release: sh -c "python manage.py migrate"
  worker: celery -A hloot worker --beat --loglevel=info
